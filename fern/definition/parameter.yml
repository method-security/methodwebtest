types:
  ParameterLocation:
    enum:
      - QUERY
      - PATH
      - HEADER
      - BODY

  Parameter:
    properties:
      location: ParameterLocation
      name: string
      value: optional<string>
